#Use xredender for battery eficiency on integrated graphics
backend = "xrender";

#Disable heavy features
shadow = false;
fading = false;
blur-background = false;

#Fix screnn tearing without heavy GPU laod
vsync = true;
